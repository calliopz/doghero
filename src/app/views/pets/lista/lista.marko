<html>
    <head>
        <meta charset="utf-8">
    </head>
    <body>
        <main class="conteudoPrincipal">
            <div class="container">
                <h1> Lista de pets </h1>

            <table id="pets" class="table table-striped table-hover">
                    <thead class="thead-dark">
            <tr>
                <td>Nome</td>
                <td>Espécie</td>
                <td>Idade</td>
                <td>Tamanho</td>
            </tr>
                    </thead>
                    <tbody>
                        <tr id="pet_${pet.id}" for (pet in data.pets)>
                            <td>${pet.id}</td>
                            <td>${pet.nome}</td>
                            <td>${pet.especie}</td>
                            <td>${pet.idade}</td>
                            <td>${pet.tamanho}</td>
                            <td><a href="/pets/form/${pet.id}">Editar</a></td>
                            <td><a href="#" data-ref="${pet.id}" data-type="remocao">Remover</a></td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </main>

        <script src="/estatico/js/remove-pet.js">
        </script>
    </body>
</html>